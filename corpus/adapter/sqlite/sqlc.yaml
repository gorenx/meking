version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "statements.sql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_empty_slices: true
        emit_interface: true
